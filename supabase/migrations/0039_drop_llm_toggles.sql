-- Переключатели «Оценка от ИИ» и «Объяснения от ИИ» убраны.
--
-- Они выбирали между двумя механиками оценки: поэлементным подсчётом по
-- эталону и разбором от модели. Второй механики больше нет — точнее, нет
-- ПЕРВОЙ: поэлементный подсчёт и разбор по элементам эталона удалены
-- целиком, оценивает мультимодальная модель и только она.
--
-- Переключатель, у которого осталось одно положение, — это не настройка, а
-- обещание выбора, которого нет. Колонки поэтому уходят вместе с ним.
--
-- ЧТО НЕ УДАЛЯЕТСЯ: сами разборы (grammar_errors), рейтинги, история. Здесь
-- снимаются ровно два булевых поля профиля.

alter table users
  drop column if exists llm_scoring_enabled,
  drop column if exists llm_explanations_enabled;
