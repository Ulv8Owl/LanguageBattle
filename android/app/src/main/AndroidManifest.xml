<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <!-- Напоминания переживают перезагрузку телефона. Без этого права
         система не скажет приложению о загрузке, все заведённые
         будильники после перезагрузки пропадут, и напоминания молча
         прекратятся до следующего запуска приложения. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- С Android 13 уведомления без этого права не показываются вовсе,
         и спросить его обязано приложение. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:label="Chrolingo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- КТО ПОКАЗЫВАЕТ ВЕЧЕРНЕЕ НАПОМИНАНИЕ. Приложения в этот
             момент нет: есть будильник и этот receiver. Не объявишь —
             сборка пройдёт, будильник заведётся, а будить будет некого:
             уведомление просто не придёт, без единой ошибки.

             Он же ловит перезагрузку: система забывает все заведённые
             будильники, и восстановить их может только приложение. -->
        <receiver
            android:name=".ReminderAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- ВИДЖЕТ НА РАБОЧЕМ СТОЛЕ. exported="true" здесь обязателен:
             широковещательное APPWIDGET_UPDATE присылает система, и
             закрытый receiver его не получит — виджет просто никогда не
             обновится. Так написано и в образце документации. -->
        <receiver
            android:name=".ChrolingoWidget"
            android:label="@string/widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/chrolingo_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Видимости системного распознавателя речи здесь СОЗНАТЕЛЬНО нет:
             речь распознаётся на сервере (supabase/functions/_shared/
             transcribeAudio.ts), приложение только пишет и загружает звук.
             Не возвращайте android.speech.RecognitionService — приложение
             не должно иметь возможности распознавать речь само. -->
    </queries>
</manifest>
